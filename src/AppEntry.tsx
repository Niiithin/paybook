/* Imports */
import React, {useEffect, useRef, useState} from 'react';
import {Text} from 'react-native';
import NoInternet from './components/NoInternet';

const AppEntry = () => {
  return <Text>AppEntry</Text>;
};

export default AppEntry;
